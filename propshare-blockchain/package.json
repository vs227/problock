{
  "name": "propshare-blockchain",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "rimraf cache artifacts",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:amoy": "hardhat run scripts/01_deploy.js --network amoy",
    "verify:amoy": "hardhat verify --network amoy",
    "deploy:polygon": "hardhat run scripts/01_deploy.js --network polygon",
    "verify:polygon": "hardhat verify --network polygon",
    "dist:new": "hardhat run scripts/02_new_distribution.js --network amoy",
    "claim": "hardhat run scripts/03_claim.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.26.3",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6"
  }
}
